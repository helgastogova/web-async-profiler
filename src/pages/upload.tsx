import { UploadReport } from '@client/upload-report';
import { BasePage } from '@client/layout';

const IndexPage = () => {
  return (
    <BasePage>
      <UploadReport />
    </BasePage>
  );
};

export default IndexPage;
